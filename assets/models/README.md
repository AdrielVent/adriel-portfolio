# Model Placeholders

Export SolidWorks/NX models as lightweight `.glb` files and place them here:

- `c-frame.glb`
- `heatsink.glb`

The portfolio already references these paths through `<model-viewer>`.
